import { hoverPointer } from "@/app/utils/styles";
import React from "react";

export default function Logo (){
    return(<p className={`${hoverPointer} text-colorColor font-bold text-xl`}>D.<span className="text-colorLight3">SHOP</span></p>)
}